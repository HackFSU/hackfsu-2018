version: '2'
services:
    webapp:
        extends:
            file: .docker/base.yml
            service: webapp
        command: sh -c "yarn install && yarn dev"
            # on windows, add `--no-bin-links` after `yarn install`
            # DO NOT COMMIT THIS CHANGE
        ports:
            - "3000:3000"
        volumes:
            - ./webapp:/usr/src/app
